# while_loop_python
making strong concept about while loop in python.
This is a simple Python program that takes a user's name as input and ensures that the input is not empty or only spaces.

## 🚀 Features
- Removes extra spaces using `.strip()`
- Prevents empty input
- Keeps asking user until valid name is entered
- Displays a welcome message after valid input
